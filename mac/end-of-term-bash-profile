alias node14='export PATH="/usr/local/opt/node@14/bin:$PATH"'

# export PATH="/usr/local/opt/python@3.7/bin:$PATH"

PROFILE=~/.bash_profile
BASHRC=~/.bashrc

# BASH ALIASES
#alias show_cmd="cat $PROFILE | grep 'curr'"
alias change_pass="./run_manage.sh -d antonio -c 'changepassword system@procurify.com'"
alias django_shell="source .aliases && dcp run uwsgi sh"
alias activate_venv="source venv/bin/activate"
alias run_django="python manage.py runserver"
alias py="python3"
alias rl="touch uwsgi.reload"
#alias restart="docker-compose restart uwsgi"
alias debug="source .aliases && dcp up -d uwsgi"
alias restart="source .aliases && dcp up -d uwsgi"
alias django="python manage.py" 
alias unzip_tgz="tar -xvzf"
alias curr="yarn test:watch --runInBand --collectCoverage=false /Users/sidneyl/procurify-react/packages/webapp/src/components/PaymentSyncStatusLabel/__tests__/PaymentSyncStatusLabel.feature.tsx"
# function change_curr() {
# 	sed -i --debug "s/alias curr='.*'/alias curr='$1'/g" $PROFILE
# }
alias test="./run_tests.sh"



# GIT ALIASES
alias ga="git add"
alias gc="git commit -m"
alias gac="git add . && git commit -m"
alias gp="git push origin HEAD"
alias gpf="git push --force-with-lease origin HEAD"
# # all arguments except last are file paths to add
# # last argument is commit message
# function gac() {
#   git add "${@:1:$#-1}";
#   git commit -m "${@: -1}"
# }
function glazy() {
    git add . 
    git commit -m "$1"
    git push origin HEAD
}

alias gpl="git pull"

alias gbr="git branch"
alias gbrD="git branch -D"

alias gst="git status"
alias gl="git log --oneline"  # could be improved
alias gdf="git diff"

alias gco="git checkout"
alias gcob="git checkout -b"

# what does this do exactly? I know it has something to do with unstaged files 
alias gpristine="git reset --hard && git clean -dfx" 
alias gamend="git commit --amend -m"

alias gunstage="git reset --soft HEAD^"
alias gdiscard="git reset --hard"

alias grb="git rebase"
alias grbi="git rebase --interactive"
alias grba="git rebase --abort"
alias grbc="ga . && git rebase --continue"

alias gs="git stash"

if [ -f ~/.git-completion.bash ]; then
  . ~/.git-completion.bash
fi

__git_complete gco _git_checkout
__git_complete ga _git_add
if [ -e /Users/sidneyl/.nix-profile/etc/profile.d/nix.sh ]; then . /Users/sidneyl/.nix-profile/etc/profile.d/nix.sh; fi # added by Nix installer
